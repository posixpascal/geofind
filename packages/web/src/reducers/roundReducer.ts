import {ROUND_END, ROUND_START} from "../actions/types";

// @ts-ignore
export default function(state = {}, action) {
    switch (action.type) {
        case ROUND_START:
            break;

        case ROUND_END:
            break;
    }

    return state;
}
